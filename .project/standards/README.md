# Focus-Hub Standards (project-local overrides)

Project-specific standards overrides for Focus-Hub. Global standards live in `../../.global/standards/` and are loaded via the `opencode.json` instructions glob.

Phase 1: empty — global standards (`node-rules.md`, `mobile-rules.md`, `frontend-rules.md`) apply. Populate this folder with focus-hub-specific overrides during implementation (e.g. `sqlite-rules.md`, `sync-rules.md`).
