# Focus-Hub Guidelines (project-local)

Project-specific execution guidelines, safety constraints, and operational rules for Focus-Hub. Global guidelines live in `../../.global/guidelines/` and are loaded via the `opencode.json` instructions glob.

Phase 1: empty — global guidelines (`caveman-mode.md`, `agent-onboarding.md`, `security-rules.md`) apply. Populate this folder with focus-hub-specific rules (e.g. local-sync rules, db-schema constraints) during implementation.
