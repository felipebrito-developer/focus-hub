# Focus-Hub MCP (project-local servers)

Project-specific MCP server configurations for Focus-Hub. Global MCP config lives in `../../.global/mcp/`.

Phase 1: empty — no project-local MCP servers yet. Add server configs here as the service layer (NodeJS backend / MCP server per `docs/Roadmaps/service.md`) is implemented.
